（优先用convert_excel_to_md.py脚本，如果脚本不能满足需求，再补充功能，尽量把此脚本打造成通用脚本
）将excel中的每一个sheet都识别出来，输出markdown文件，里面内容用表格的，我们也用表格（有合并单元格的需要遵循mdtable规则），表格首列如果整列全部为空，则该列不显示，sheet名为主干标题（二级），其余内容往里面填充。一个excel文件对应一个输出md文件。如果是html文件，把也需要尽量把相应的表格识别出来，输出markdown文件。